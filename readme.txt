to run the product use
mvn clean install
mvn spring-boot:run -Drun.jvmArguments='-Dserver.port=8088'

The file RestCalls.txt contains the data and rest calls to use the solution.

The system comes with three audiologists having ids 1,2,3. Use http://localhost:8088/audiologists to see audiologists list.


REMOVE ME: http://localhost:8088/audibene/console